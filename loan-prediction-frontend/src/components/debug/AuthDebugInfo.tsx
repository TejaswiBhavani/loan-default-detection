import React from 'react';

// Debug overlay intentionally disabled in UI.
// Keep the component so imports do not break, but render nothing.
const AuthDebugInfo: React.FC = () => null;

export default AuthDebugInfo;