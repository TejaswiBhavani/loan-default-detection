# ==============================================================================
# RAILWAY DEPLOYMENT ENVIRONMENT VARIABLES
# Copy these values to your Railway service variables
# ==============================================================================

# Essential Variables (REQUIRED)
NODE_ENV=production
PORT=5000

# Database (Railway provides this automatically)
# DATABASE_URL=postgresql://postgres:password@host:port/railway

# JWT Secrets (GENERATE SECURE VALUES!)
JWT_SECRET=your_super_secure_jwt_secret_at_least_32_characters_long_change_this_now
REFRESH_TOKEN_SECRET=your_super_secure_refresh_secret_different_from_jwt_secret_change_this

# JWT Configuration
JWT_EXPIRES_IN=24h
JWT_REFRESH_EXPIRES_IN=7d

# CORS (Update with your frontend URL)
CORS_ORIGIN=https://your-frontend-domain.vercel.app,http://localhost:3000

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=info

# ==============================================================================
# RAILWAY SETUP INSTRUCTIONS:
# 1. Go to your Railway service → Variables tab
# 2. Add each variable above (except DATABASE_URL which Railway provides)
# 3. Generate secure JWT secrets using: openssl rand -base64 32
# 4. Update CORS_ORIGIN with your actual frontend domain
# 5. Redeploy your service
# ==============================================================================